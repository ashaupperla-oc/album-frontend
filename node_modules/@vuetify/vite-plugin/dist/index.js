"use strict";
const importPlugin_1 = require("./importPlugin");
const stylesPlugin_1 = require("./stylesPlugin");
module.exports = function vuetify(_options = {}) {
    const options = {
        autoImport: true,
        styles: true,
        stylesTimeout: 10000,
        ..._options,
    };
    const plugins = [];
    if (options.autoImport) {
        plugins.push((0, importPlugin_1.importPlugin)());
    }
    if (options.styles === 'none' || options.styles === 'expose') {
        plugins.push((0, stylesPlugin_1.stylesPlugin)(options));
    }
    return plugins;
};
//# sourceMappingURL=index.js.map